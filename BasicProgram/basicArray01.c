#include <stdio.h>
int main()
{
    int t = 10000000;
    int arr[10000000];
    // scanf("%d %d", &t, &arr[t]);
    for (int i = 0; i <= t; ++i)
    {
        // printf("%d\n", arr[]);
        arr[i] = i;
    }
    return 0;
}