#include <stdio.h>
int main()
{
    int i = 1;      // variable declarations and initializations
    while (i <= 10) // condition
    {
        printf("I Will Do It\n");
        i++; // update
    }
    return 0;
}